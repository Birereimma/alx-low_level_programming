#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@b: the integer to be computed
 * Return: the absolute value
 */

int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}

